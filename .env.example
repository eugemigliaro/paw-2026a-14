# Runtime database configuration for the webapp.
# Export these variables in the Tomcat process before deploying the WAR.

PAW_DB_URL=jdbc:postgresql://localhost/paw
PAW_DB_USERNAME=your_db_username
PAW_DB_PASSWORD=your_db_password
